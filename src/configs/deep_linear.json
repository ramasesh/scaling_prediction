{
  "model_config": {
    "arch": "deep_linear",
    "depth": 10,
    "width": 20,
    "use_bias": "true"
  },
  "optim_config": {
    "optimizer": "sgd",
    "base_lr": 0.1,
    "momentum": 0.9,
    "weight_decay": 1e-4,
    "nesterov": true,
    "epochs": 160,
    "scheduler": "None",
    "batch_size": 128
  }
}
