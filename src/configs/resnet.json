{
  "model_config": {
    "arch": "resnet",
    "base_channels": 32,
    "block_type": "basic",
    "depth": 18
  },
  "optim_config": {
    "optimizer": "sgd",
    "base_lr": 0.1,
    "momentum": 0.9,
    "weight_decay": 1e-4,
    "nesterov": true,
    "epochs": 160,
    "scheduler": "None",
    "batch_size": 128
  }
}
